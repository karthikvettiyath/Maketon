# Root env (optional). Server will also load server/.env if present.

# Server
PORT=61234
CLIENT_ORIGIN=http://localhost:5173
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Client (only needed if you decide to call Supabase directly from the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
